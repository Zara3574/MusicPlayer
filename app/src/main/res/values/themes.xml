<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!--         Customize your light theme here.-->
        <item name="colorPrimary">@color/red</item>
        <item name="colorOnPrimary">@color/black</item> <!-- Text color on primary -->
        <item name="colorSurface">@color/white</item> <!-- Background color -->
        <item name="colorOnSurface">@color/black</item> <!-- Text color on white background -->
    </style>

    <style name="Theme.MusicPlayer" parent="Base.Theme.MusicPlayer">
        <item name="colorPrimary">@color/red</item>
    </style>

    <style name="splash_screen" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="colorPrimary">@color/red</item>  <!-- Red Toolbar -->
        <item name="colorPrimaryVariant">@color/red</item> <!-- Optional -->
        <item name="android:statusBarColor">@color/red</item> <!-- Status Bar Red -->
    </style>

</resources>