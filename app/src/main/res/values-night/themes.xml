<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/red</item>
        <item name="colorOnPrimary">@color/white</item> <!-- Text color on primary -->
        <item name="colorSurface">@color/black</item> <!-- Background color -->
        <item name="colorOnSurface">@color/white</item> <!-- Text color on white background -->
    </style>
</resources>